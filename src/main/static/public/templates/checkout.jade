!!! 5
html
    head
        title=pageName
    body
        div
            p: a(href="/") Home

        div
            p: strong Checkout
        div
            ul#basket
                for book in basket
                    li
                        | #{book.id} -
                        a(href="/books/#{book.id}")=book.name
                        =' '
                        | for £ #{book.price}
                        =' '
                        form(action="/basket/delete", method="post")
                            input(type="hidden", name="bookId", value="#{book.id}")
                            input(type="submit", value="remove")
        div
            p Total: #{total}

        div
            p: strong Payment details
                form(name="payment", action="/payment", method="post")
                    div
                        dt: span Full name
                        dd: input(type="text", name="Name", required)
                        dt: span Address
                        dd: input(type="text", name="address", required)
                        dt: span Card details
                        dd: input(type="text", name="card_details", required)
                    div
                        input(type="submit", value="Pay now")
